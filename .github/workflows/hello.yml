name: Hello World!

on:
  push:
    branches: [ main ]

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hello
      run: |
        echo "Hello, world!"
    - name: Now it is
      run: |
        date
    - name: Directory content
      run: |
        ls